# OMINO TENSOR MODEL: AUTOMATED DEVELOPER AGENT MANDATE
# CLASSIFICATION: LAYER 0 SUBSTRATE FOOTPRINT LOCK

## 1. Component Isolation

Operate strictly inside this repository context. Do not introduce high-level
network structures, application canvas logic, or outside lisp parsers. All
standalone substrate verification is driven through the root `make verify`
target.

## 2. Flat Insulated Substrate Registry

All machine-readable substrate files (`.omi`, `.imo`, `.o`, `.hs`, `.h`, and
`.c`) must remain flat within the project-local `./.omi/` directory (expected
count: 23). The visible root and `./docs/` directory are reserved for
human-readable Markdown files with front-matter bindings. No internal subfolder
nesting is permitted under `./.omi/` or `./docs/`.

## 3. Primitive Tensor Invariants

Every local edit must preserve the active checks for 16-bit word footprint
masking, the parabolic nomogram zero-origin balance, the `4320` local incidence
scale, and the five canonical substrate target names.
