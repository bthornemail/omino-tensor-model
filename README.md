# OMINO Tensor Model

Layer 0 substrate-core repository for the OMI workspace.

This repository is currently registered as an independent project boundary. Its
source content will be populated by the OMINO tensor meta-compilation path.

Initial role:

```text
Layer 0: Substrate Core
16-bit OMINO footprint
4320 local incidence surface
4320^2 interaction surface
4320^4 meta-matrix horizon
```

Do not treat this repository as generated by the root workspace directly. It is
tracked as a submodule so the root project can pin the substrate-core boundary
without collapsing it into the parent repository.
